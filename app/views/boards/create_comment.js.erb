// 5. 댓글을 보여주는 table의 tbody에 추가해 줌.
$('.comment-list').prepend(`
    <tr id="comment-<%= @comment.id %>">
      <td><%= @comment.contents %></td>
      <td></td>
      <td><button class="btn btn-outline-danger delete-comment" data-id="<%= @comment.id %>"><i class="fas fa-edit fa-spin"></i>삭제</button></td>
    </tr>
  `);
// 6. input 태그를 비워줌
$('#comment-input').val('');
