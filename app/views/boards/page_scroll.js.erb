alert("페이지 추가할께요.");
$('.boards').append(`
    <% @boards.each do |board| %>
      <tr class="content" data-id="<%= board.id %>">
        <td><%= board.id %></td>
        <td class="title"><%= board.title %></td>
      </tr>
    <% end %>
  `)
